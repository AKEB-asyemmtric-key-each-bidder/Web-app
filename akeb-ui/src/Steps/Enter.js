import { Button } from 'antd'
import React from 'react'

const Enter = () => {
    return (
        <Button type="primary" size='large'>
            Enter the auction!
        </Button>
    )
}

export default Enter